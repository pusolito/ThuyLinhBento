package com.thuylinh.bento.models

enum class TrackStatus {
    PENDING,
    DELIVERED,
    SHIPPED,
    CANCELLED,
    ARRIVED,
    OUTFORDELIVERY,
    NOTDONE,
    DELIVERING,
    pending, processing, on_hold, completed, cancelled, refunded, failed, trash
}